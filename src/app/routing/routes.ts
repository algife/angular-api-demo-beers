import { Routes } from '@angular/router';
import { BeerCatalogComponent } from '@components/beer-catalog/beer-catalog.component';

export const routes: Routes = [{ path: '', component: BeerCatalogComponent }];
